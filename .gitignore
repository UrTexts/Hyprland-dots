# ── Build / archive junk ─────────────────────────────────────
*.iso
*.zip
*.tar.gz
*.log
.cache/

# ── Scripts (personal, not for others) ───────────────────────
sync.sh

# ── Legcord (entire dir — cache, session, GPU data, etc.) ────
config/legcord/

# ── Spotify ──────────────────────────────────────────────────
config/spotify/

# ── Unity / game saves ───────────────────────────────────────
config/unity3d/

# ── Go telemetry ─────────────────────────────────────────────
config/go/

# ── Heroic ───────────────────────────────────────────────────
config/heroic/

# ── PulseAudio cookie (machine-specific binary) ──────────────
config/pulse/

# ── Session files ─────────────────────────────────────────────
config/session/

# ── ncspot user state (listening history, cbor cache) ────────
config/ncspot/userstate.cbor

# ── yt-x recent history ──────────────────────────────────────
config/yt-x/recent.json

# ── MPV logs ─────────────────────────────────────────────────
config/mpv/scripts/log.txt

# ── dconf binary dump (machine-specific) ─────────────────────
config/dconf/

# ── KDE/Dolphin session state ────────────────────────────────
config/dolphinrc
config/filetypesrc
config/trashrc
config/mimeapps.list

# ── Qt session ───────────────────────────────────────────────
config/qt5ct/
config/qt6ct/

# ── GTK (generated by matugen, no need to track) ─────────────
config/gtk-3.0/
config/gtk-4.0/

# ── matugen (generated files) ────────────────────────────────
config/matugen/

# ── SwayOSD (no config needed) ───────────────────────────────
config/swayosd/

# ── Mozilla ──────────────────────────────────────────────────
config/mozilla/

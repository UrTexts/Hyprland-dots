# ── Build / archive junk ─────────────────────────────────────
*.iso
*.zip
*.tar.gz
*.log
.cache/

# ── Scripts (personal, not for others) ───────────────────────
sync.sh

# ── Legcord ──────────────────────────────────────────────────
config/legcord/

# ── Spotify ──────────────────────────────────────────────────
config/spotify/

# ── Unity / game saves ───────────────────────────────────────
config/unity3d/

# ── Go telemetry ─────────────────────────────────────────────
config/go/

# ── Heroic ───────────────────────────────────────────────────
config/heroic/

# ── PulseAudio cookie (machine-specific binary) ──────────────
config/pulse/

# ── dconf binary dump (machine-specific) ─────────────────────
config/dconf/

# ── ncspot user state (listening history, cbor cache) ────────
config/ncspot/userstate.cbor

# ── yt-x recent history ──────────────────────────────────────
config/yt-x/recent.json

# ── MPV logs ─────────────────────────────────────────────────
config/mpv/scripts/log.txt

# ── KDE/Dolphin junk ─────────────────────────────────────────
config/dolphinrc
config/filetypesrc
config/trashrc
config/mimeapps.list
config/kde.org/
config/menus/

# ── yay cache/config ─────────────────────────────────────────
config/yay/

# ── Qt session ───────────────────────────────────────────────
config/qt5ct/
config/qt6ct/

# ── GTK (generated by matugen) ───────────────────────────────
config/gtk-3.0/
config/gtk-4.0/

# ── matugen (generated files) ────────────────────────────────
config/matugen/

# ── SwayOSD (no config needed) ───────────────────────────────
config/swayosd/

# ── Mozilla ──────────────────────────────────────────────────
config/mozilla/
